import 'package:flutter/material.dart';

class AppColors {
  // Brand Government KLH / BPLH
  static const Color primary = Color(0xFF1B5E20);        // Deep Forest Green
  static const Color primaryLight = Color(0xFF2E7D32);   // Emerald Green
  static const Color primaryDark = Color(0xFF0F3D14);
  static const Color primaryContainer = Color(0xFFDCFCE7);

  static const Color secondary = Color(0xFF0D47A1);      // Policy Blue
  static const Color secondaryLight = Color(0xFF1565C0);
  static const Color secondaryContainer = Color(0xFFDBEAFE);

  // Status & Indikator Medis B3
  static const Color hazardRed = Color(0xFFC62828);      // Defisit Kritis / Medis
  static const Color warningOrange = Color(0xFFF57F17);  // Defisit Sedang
  static const Color successGreen = Color(0xFF2E7D32);   // Surplus / Berizin
  static const Color coldStorageBlue = Color(0xFF0284C7);// Depo Pemindahan / Cold storage

  // Surface & Neutral Colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color card = Colors.white;
  static const Color border = Color(0xFFE2E8F0);

  // Typography Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
}
